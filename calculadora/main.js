const calculator1 = new Calculator()  
const calculator2 = new Calculator()  

document.addEventListener('DOMContentLoaded', 
    () => {
        
        document.body.append(calculator1.htmlElement);
        document.body.append(calculator2.htmlElement);

    })
  
  
  
